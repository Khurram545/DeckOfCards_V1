namespace DeckOfCards.Models
{
    /// <summary>
    /// Suits enum used to generate weight as well
    /// </summary>
    public enum enumCardSuit
    {
        // base values to calculate series
        Club = 1,

        Diamond = 20,
        Heart = 30,
        Spade = 40,
    }
}